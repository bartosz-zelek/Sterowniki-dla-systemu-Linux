// -*- mode: C++; c-file-style: "virtutech-c++" -*-

/*
  © 2025 Intel Corporation

  This software and the related documents are Intel copyrighted materials, and
  your use of them is governed by the express license under which they were
  provided to you ("License"). Unless the License provides otherwise, you may
  not use, modify, copy, publish, distribute, disclose or transmit this software
  or the related documents without Intel's prior written permission.

  This software and the related documents are provided as is, with no express or
  implied warranties, other than those that are expressly stated in the License.
*/

// This file is generated by the script bin/gen-cc-interface

#ifndef SIMICS_CPP_MODEL_IFACE_DECODER_H
#define SIMICS_CPP_MODEL_IFACE_DECODER_H

#include "simics/model-iface/decoder.h"

#include <simics/detail/conf-object-util.h>  // get_interface
#include <simics/iface/interface-info.h>

namespace simics {
namespace iface {

class DecoderInterface {
  public:
    using ctype = decoder_interface_t;

    // Function override and implemented by user
    virtual void register_decoder(decoder_t *decoder) = 0;
    virtual void unregister_decoder(decoder_t *decoder) = 0;

    // Function convert C interface call to C++ interface call
    class FromC {
      public:
        static void register_decoder(conf_object_t *obj, decoder_t *decoder) {
            detail::get_interface<DecoderInterface>(obj)->register_decoder(decoder);
        }
        static void unregister_decoder(conf_object_t *obj, decoder_t *decoder) {
            detail::get_interface<DecoderInterface>(obj)->unregister_decoder(decoder);
        }
    };

    // Function convert C++ interface call to C interface call
    class ToC {
      public:
        ToC() : obj_(nullptr), iface_(nullptr) {}
        ToC(conf_object_t *obj, const DecoderInterface::ctype *iface)
            : obj_(obj), iface_(iface) {}

        void register_decoder(decoder_t *decoder) const {
            iface_->register_decoder(obj_, decoder);
        }
        void unregister_decoder(decoder_t *decoder) const {
            iface_->unregister_decoder(obj_, decoder);
        }

        const DecoderInterface::ctype *get_iface() const {
            return iface_;
        }

      private:
        conf_object_t *obj_;
        const DecoderInterface::ctype *iface_;
    };

    class Info : public InterfaceInfo {
      public:
        // InterfaceInfo
        std::string name() const override { return DECODER_INTERFACE; }
        const interface_t *cstruct() const override {
            static constexpr DecoderInterface::ctype funcs {
                FromC::register_decoder,
                FromC::unregister_decoder,
            };
            return &funcs;
        }
    };
};

}  // namespace iface
}  // namespace simics

#endif
