%YAML 1.2
---
description: >-
  Intel Simics Internals Training, target for the time quantum lab, 
  with multiple systems in the same simulation. 
params:
  ribit_one:
    import: "%script%/05-time-quanta.target.yml"
    defaults:
      system:
        name: ribit_one
  ribit_two:
    import: "%script%/05-time-quanta.target.yml"
    defaults:
      system:
        name: ribit_two

script: "%script%/05-time-quanta-multi.target.yml.include"
...
